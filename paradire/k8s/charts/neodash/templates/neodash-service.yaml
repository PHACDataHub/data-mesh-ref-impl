apiVersion: v1
kind: Service
metadata:
  name: neodash-service
spec:
  selector:
    app: neodash-deployment
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
