/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Removing Vaccination Records
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
CALL apoc.periodic.iterate('
	MATCH (n:VaccinationRecord)
	RETURN n
','
    WITH n DETACH DELETE n
',
	{batchSize:10000, parallel:true}
) YIELD total RETURN total;
//

//
// Removing Patient-Immunization statistics
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
CALL apoc.periodic.iterate('
	MATCH (n:ZipImmunization)
	RETURN n
','
    WITH n DETACH DELETE n
',
	{batchSize:10000, parallel:true}
) YIELD total RETURN total;
//

//
// Removing Top-K Immunization per Zip Code statistics
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
CALL apoc.periodic.iterate('
	MATCH (n:TopKImmunization)
	RETURN n
','
    WITH n DETACH DELETE n
',
	{batchSize:10000, parallel:true}
) YIELD total RETURN total;
//

//
// Removing PatientCvxOrg statistics
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
CALL apoc.periodic.iterate('
	MATCH (n:PatientCvxOrg)
	RETURN n
','
    WITH n DETACH DELETE n
',
	{batchSize:10000, parallel:true}
) YIELD total RETURN total;
//

//
// Removing CityYearTopProc statistics
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
CALL apoc.periodic.iterate('
	MATCH (n:CityYearTopProc)
	RETURN n
','
    WITH n DETACH DELETE n
',
	{batchSize:10000, parallel:true}
) YIELD total RETURN total;
//

//
// Removing PtOrgMed statistics
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
CALL apoc.periodic.iterate('
	MATCH (n:PtOrgMed)
	RETURN n
','
    WITH n DETACH DELETE n
',
	{batchSize:10000, parallel:true}
) YIELD total RETURN total;
//

//
// Removing CityOrgPatient statistics
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
CALL apoc.periodic.iterate('
	MATCH (n:CityOrgPatient)
	RETURN n
','
    WITH n DETACH DELETE n
',
	{batchSize:10000, parallel:true}
) YIELD total RETURN total;
//

//
// Removing CityOrgPatientVisit statistics
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
CALL apoc.periodic.iterate('
	MATCH (n:CityOrgPatientVisit)
	RETURN n
','
    WITH n DETACH DELETE n
',
	{batchSize:10000, parallel:true}
) YIELD total RETURN total;
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

